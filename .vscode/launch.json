{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "justMyCode": true,
      "envFile": "${workspaceFolder}/.env.debug"
    },
    {
      "name": "Docker: Image Classifier API",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug ic",
      "python": {
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/classifierapi/app",
            "remoteRoot": "/app"
          }
        ],
        "projectType": "general"
      }
    }
  ]
}
